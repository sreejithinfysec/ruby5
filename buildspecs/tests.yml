version: 0.2

batch:
  fast-fail: false
  build-list:
    - identifier: tests1
      buildspec: buildspecs/tests1.yml
    - identifier: tests2
      buildspec: buildspecs/tests2.yml

phases:
  install:
    runtime-versions:
      ruby: 2.7
    commands:
      - echo "I'm alive!"
      - pwd
      - git diff

  pre_build:
    commands:
      - gem install bundler
      - bundle install

  build:
    commands:
      - bundle exec rspec

artifacts:
  files:
    - "**/*"
